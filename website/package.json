{
  "name": "localgraph-docs",
  "version": "1.0.0",
  "description": "LocalGraph - AI Agents 101: Build, Learn & Experiment Locally",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run build:jupyterlite && vitepress build",
    "build:vitepress": "vitepress build",
    "build:jupyterlite": "cd jupyterlite && ./build.sh",
    "preview": "vitepress preview",
    "deploy": "npm run build && gh-pages -d .vitepress/dist"
  },
  "devDependencies": {
    "vitepress": "^1.3.4",
    "vue": "^3.4.38",
    "vitepress-plugin-mermaid": "^2.0.16",
    "mermaid": "^10.9.1",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "@vueuse/core": "^10.11.1"
  }
}
